---
description: "Search the web and return structured results"
argument-hint: "<query> [--count N] [--time d|w|m|y]"
allowed-tools:
  - Bash
---

Run the web search script with the user's arguments and present the results.

Execute this command:

```
python xLoop/scripts/web_search.py $ARGUMENTS
```

Present the output directly to the user. If the script reports an error, explain it and suggest fixes.
