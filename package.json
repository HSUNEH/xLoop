{
  "name": "xloop",
  "version": "0.2.0",
  "description": "xLoop Agent Harness — Claude Code plugin for research-integrated planning + self-improvement",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["agent", "harness", "claude-code", "plugin", "orchestration"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
