{
  "name": "xloop",
  "version": "0.3.0",
  "description": "xLoop Agent Harness — Claude Code plugin for research-integrated planning + self-improvement",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "bundle:mcp": "esbuild src/mcp-server.ts --bundle --platform=node --target=node18 --format=cjs --outfile=bridge/mcp-server.cjs",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["agent", "harness", "claude-code", "plugin", "orchestration"],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
